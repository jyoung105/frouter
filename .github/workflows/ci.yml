name: CI

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  test:
    name: Test (Node ${{ matrix.node }} on ${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest]
        node: [20, 22]

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node }}

      - name: Install dependencies
        run: npm install

      - name: Typecheck
        run: npm run typecheck

      - name: Run tests
        run: node --test dist/tests/unit/*.test.js dist/tests/integration/*.test.js dist/tests/security/*.test.js

      - name: Perf baseline + perf tests
        if: matrix.os == 'ubuntu-latest' && matrix.node == 22
        run: |
          npm run perf:baseline
          npm run test:perf
